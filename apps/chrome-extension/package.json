{
  "name": "@verve/chrome-extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build"
  },
  "dependencies": {
    "@verve/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
